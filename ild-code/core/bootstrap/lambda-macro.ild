(mk-lambda
  (free-vars)       ; this can be replaced with a closure that has just cons, mk-lambda, quote and free-vars
  (quote (arg-names body))
  (quote
    (cons mk-lambda
      (cons (free-vars)
        (cons arg-names
          (cons body
            (quote ())))))))
